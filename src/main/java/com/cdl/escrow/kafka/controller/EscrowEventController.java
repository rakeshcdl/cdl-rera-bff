/*
package com.cdl.escrow.kafka.controller;

import com.cdl.escrow.kafka.dto.EscrowEvent;
import com.cdl.escrow.kafka.service.EscrowEventProducer;
import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/api/v1/events")
@Slf4j
@RequiredArgsConstructor
public class EscrowEventController {

   */
/* private final EscrowEventProducer producer;

    @PostMapping
    public ResponseEntity<Void> publish(@RequestBody EscrowEvent event) {
        producer.send(event);
        return ResponseEntity.accepted().build();
    }*//*

}
*/
